body {
    margin: 0;
}

header {
    background-color: #ccc;
    height: 100px;
}

.container {
    display: flex;
    flex-direction: row;
    height: calc(100vh - 200px);
}

nav {
    background-color: #eee;
    flex: 1;
}

main {
    background-color: #ddd;
    flex: 2;
}

aside {
    background-color: #ccc;
    flex: 1;
}

footer {
    background-color: #ccc;
    height: 100px;
}
.video-container {
  position: relative;
  padding-bottom: 56.25%;
  /* Set the height to 0 to allow the padding-bottom to create the aspect ratio */
  height: 0;
}

.video-container video {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
.contact-form form label,
.contact-form form input[type="text"],
.contact-form form textarea,
.contact-form form button {
  display: block;
  width: 100%;
  margin-bottom: 10px;
}
